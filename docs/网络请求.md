# 网络请求

通过编写网络请求，能够使扩展程序与其他应用进行通信。更新 `service-worker.js` 文件内容如下。

```javascript
async function func() {
    try {
        let data = await fetch("https://api.example.com/");
        console.log(data.json());
    } catch (error) {
        console.warn(error);
    }
}
func();
```

